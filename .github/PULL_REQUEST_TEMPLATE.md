## What changed and why

## Which invariant or ADR this touches, if any

`AGENTS.md`'s hard rules and `docs/decisions/` explain most design choices. If this changes one, say which, and update the ADR or add a new one.

## Checklist

- [ ] `pnpm run preflight` passes (lint, em-dash check, typecheck, test)
- [ ] `pnpm run fixtures:write` was run if any fixture-generating code changed, and the diff is committed
- [ ] `docs/` and `CHANGELOG.md` updated if this changes behaviour a reader would notice
- [ ] No new em-dash characters, anywhere
