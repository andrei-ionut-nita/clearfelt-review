# current-from-historical

A claim about today resting entirely on evidence about last year.

Trips `finding.current_from_historical_evidence` and nothing else.

This run passes `validate` completely. That is the point: it is a
well-formed review that a reader should not act on.

```bash
node packages/core/dist/cli.js validate fixtures/adversarial/current-from-historical
node packages/core/dist/cli.js quality  fixtures/adversarial/current-from-historical
```
