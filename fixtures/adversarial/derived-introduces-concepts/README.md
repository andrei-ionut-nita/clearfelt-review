# derived-introduces-concepts

A finding labelled derived that travelled well past its evidence.

Trips `finding.derived_introduces_concepts` and nothing else.

This run passes `validate` completely. That is the point: it is a
well-formed review that a reader should not act on.

```bash
node packages/core/dist/cli.js validate fixtures/adversarial/derived-introduces-concepts
node packages/core/dist/cli.js quality  fixtures/adversarial/derived-introduces-concepts
```
