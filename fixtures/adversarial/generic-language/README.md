# generic-language

Consultant theatre: advice that could be given to any entity at all.

Trips `recommendation.generic_language` and nothing else.

This run passes `validate` completely. That is the point: it is a
well-formed review that a reader should not act on.

```bash
node packages/core/dist/cli.js validate fixtures/adversarial/generic-language
node packages/core/dist/cli.js quality  fixtures/adversarial/generic-language
```
