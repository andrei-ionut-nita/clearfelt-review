# no-independent-corroboration

Two sources agreeing, both republishing the same original.

Trips `finding.no_independent_corroboration` and nothing else.

This run passes `validate` completely. That is the point: it is a
well-formed review that a reader should not act on.

```bash
node packages/core/dist/cli.js validate fixtures/adversarial/no-independent-corroboration
node packages/core/dist/cli.js quality  fixtures/adversarial/no-independent-corroboration
```
