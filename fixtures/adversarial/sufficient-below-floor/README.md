# sufficient-below-floor

Closed as sufficient on less evidence than the budget itself demanded.

Trips `question.sufficient_below_evidence_floor` and nothing else.

This run passes `validate` completely. That is the point: it is a
well-formed review that a reader should not act on.

```bash
node packages/core/dist/cli.js validate fixtures/adversarial/sufficient-below-floor
node packages/core/dist/cli.js quality  fixtures/adversarial/sufficient-below-floor
```
